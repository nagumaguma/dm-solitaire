[project]
name = "dm-solitaire"
version = "1.0.0"
description = "DM Solitaire Multiplayer Tool"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
