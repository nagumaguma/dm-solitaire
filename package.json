{
  "name": "dm-solitaire-local-tests",
  "version": "0.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "test": "playwright test",
    "test:search-image": "playwright test tests/search-image.spec.js",
    "test:dm-operation-ui": "playwright test tests/dm-operation-ui.spec.js",
    "prod:check": "node scripts/prod-smoke.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0"
  }
}
